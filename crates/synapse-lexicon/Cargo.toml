[package]
name = "synapse-lexicon"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Synapse Nexus — domain lexicons (system / user / dynamic)"

[dependencies]
serde.workspace = true
